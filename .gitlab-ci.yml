stages:
  - build
  - release

# --- BUILD ----------------------------------------------------------------------------------

build-debian-trixie-x64:
    stage: build
    image: python:3-slim-trixie
    tags:
        - docker-build
    variables:
        GIT_DEPTH: 0
    before_script:
        - apt update && apt install -y binutils git zlib1g-dev libjpeg-dev
        - pip install --upgrade pip
        - python --version && pip --version
    script:
        - git tag && git status
        - pip install -r requirements.txt -r requirements-dev.txt
        - pylint --version && pylint --errors-only --disable import-error src/tvthekidx/*.py
        - flake8 --version && flake8 src/tvthekidx/*.py --ignore=E501,E722 --statistics --count
        - pip install -e .
        - pyinstaller --clean tvthekidx.spec
        - dist/tvthekidx -h

# --- RELEASE --------------------------------------------------------------------------------

release-source:
    stage: release
    image: python:latest
    tags:
        - docker-deploy
    needs:
        - job: build-debian-trixie-x64
          artifacts: false
    rules:
       - if: $CI_COMMIT_TAG =~ /^([0-9]+)\.([0-9]+)\.([0-9]+)(-([0-9a-zA-Z.-]+))?$/
    variables:
        GIT_DEPTH: 0
    script:
        - pip install build twine
        - python -m build
        - TWINE_PASSWORD=${CI_JOB_TOKEN} TWINE_USERNAME=gitlab-ci-token python -m twine upload --repository-url ${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/pypi dist/*
